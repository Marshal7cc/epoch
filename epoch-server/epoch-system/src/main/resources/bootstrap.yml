spring:
  application:
    name: epoch-system
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8010
        group: EPOCH_GROUP
        prefix: epoch-system
        file-extension: yml
        refreshable-dataids:
      discovery:
        # ip: 114.67.107.190
        server-addr: localhost:8010
