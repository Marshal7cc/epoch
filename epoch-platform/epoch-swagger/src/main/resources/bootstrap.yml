spring:
  application:
    name: epoch-swagger
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: ${nacos.url:localhost}:8010
        group: EPOCH_GROUP
        prefix: epoch-swagger
        file-extension: yml
        refreshable-dataids:
      discovery:
        server-addr: ${nacos.url:localhost}:8010
