package [(${package})].service;

import org.epoch.mybatis.repository.BaseRepository;
import [(${package})].domain.entity.[(${entityName})];

/**
 * @author [(${authorName})]
 */
public interface [(${repositoryName})] extends BaseRepository<[(${entityName})]>{

}
