spring:
  application:
    name: epoch-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8010
        group: EPOCH_GROUP
        prefix: epoch-gateway
        file-extension: yml
        refreshable-dataids:
      discovery:
        server-addr: localhost:8010
